[package]
name = "test_macros"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
proc-macro = true

[dependencies]
macro_magic = { path = "../../", features = ["proc_support"] }
syn = { version = "1.0", features = ["full"] }
quote = "1.0"
derive-syn-parse = "0.1.5"
