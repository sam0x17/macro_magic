[package]
name = "example_proc_macro"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
proc-macro = true

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
macro_magic = { version = "*", path = "../../" }
example_crate = { version = "*", path = "../example_crate" }
token_watcher = { version = "*", path = "../../token_watcher" }
