[package]
name = "tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
macro_magic = { version = "*", path = "../"}
syn = { version = "1.0", features = ["full"] }
example_proc_macro = { version = "*", path = "./example_proc_macro" }
